version: '2'
services:
  contextualizer:
    build: .
    environment:
      NODE_ENV: coyote_testing
    volumes_from:
      - nodesecurity

  nodesecurity:
    image: aboutdotme/nodesecurity:latest

